angular.module("arkano_angular")
    .config([
        "$locationProvider",
        function($locationProvider){
            // $locationProvider.hashPrefix("!").html5Mode(true);
        }
    ])
    .run([
        function(){
            
        }
    ]);